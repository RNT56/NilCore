# Copy to .env (gitignored). Secrets are read from the environment ONLY —
# never committed, never written elsewhere, never placed in a prompt.

# Native backend (required)
ANTHROPIC_API_KEY=
NULLCLAW_MODEL=claude-sonnet-4-6

# Codex backend
CODEX_API_KEY=

# Channels (Phase 1)
TELEGRAM_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_BOT_TOKEN=
