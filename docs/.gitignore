# secrets — never commit
.env

# runtime artifacts
nullclaw.events.jsonl
*.db
*.sqlite

# build output
/nullclaw
/dist/

# worktrees created per task live outside the repo, but ignore stray ones
/nullclaw-*/
