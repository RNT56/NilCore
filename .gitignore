# Local env and secrets — never committed (invariant I3)
.env

# Append-only event log output
*.events.jsonl

# Built binaries
/nilcore
/cmd/nilcore/nilcore

# macOS
.DS_Store

# Local Claude Code session state (per-machine, not shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock
